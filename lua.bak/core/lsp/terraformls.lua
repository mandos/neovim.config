-- local lspcontainers = require('lspcontainers')

return {
	-- before_init = function(params)
	--     params.processId = vim.NIL
	-- end,

	-- cmd = lspcontainers.command("terraform-ls serve", {
	--     image = "mandos22/ls-terraform:1.1.9",
	-- })
}
